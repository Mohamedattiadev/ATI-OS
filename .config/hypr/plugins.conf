# generated by `ati-plugin sync` — do not edit, your changes will be overwritten
#
# Hyprland binds contributed by plugins in ~/.config/ati-plugins.
# Sourced by binds.conf. Regenerate with: ati-plugin sync
#
# This file is TRACKED and ships comment-only on purpose: Hyprland
# treats a missing source as an error and draws its red overlay over
# the desktop, so the file has to exist before any script has run.
# With no plugins installed the output below is empty and this file
# is byte-identical to what the repo ships.
