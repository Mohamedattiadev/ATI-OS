
(defwindow onboarding-finish
  :monitor 0
  :geometry (geometry
              :x "0%"
              :y "0%"
              :width "420px"
              :height "220px"
              :anchor "center")
  :stacking "fg"
  :windowtype "dialog"
  :wm-ignore false
  :exclusive false
  :focusable false

  (box
    :class "onboarding"
    :orientation "v"
    :space-evenly false

    ;; ----------------------------
    ;; Title
    ;; ----------------------------
    (label
      :class "onb-title"
      :halign "start"
      :text "You're all set")

    ;; ----------------------------
    ;; Message
    ;; ----------------------------
    (label
      :class "onb-text"
      :halign "start"
      :xalign 0
      :wrap true
      :unindent true
      :text
"Your system is ready to use now.
You’ve learned the basics:

• The top bar
• Workspaces
• Keybindings

From here on, everything is muscle memory.
Take your time, explore, and make it yours.
")

    ;; ----------------------------
    ;; Tip
    ;; ----------------------------
    ;; :wrap -- the card is 420px and this line is wider than it; without
    ;; wrapping pango ellipsised the tip mid-sentence.
    (label
      :class "onb-text"
      :halign "start"
      :xalign 0
      :wrap true
      :markup
"<b>Tip:</b> <b><span foreground='${palette.cyan}'>Win + Shift + K</span></b> then <b><span foreground='${palette.cyan}'>k</span></b> opens the cheatsheet, anytime.")

    ;; ----------------------------
    ;; Staying current
    ;; ----------------------------
    ;; The desktop is configured from a git checkout that is pushed to far
    ;; more often than the ISO is rebuilt, so an install from an older
    ;; stick is an older desktop -- and nothing on screen would ever say
    ;; so. This is the one place every new user is guaranteed to look.
    (label
      :class "onb-text"
      :halign "start"
      :xalign 0
      :wrap true
      :markup
;; Kept SHORT on purpose. :wrap true is set and still does not wrap here
    ;; -- the label sits in a fixed-width card and pango ellipsises the
    ;; overflow instead, exactly as the tip above once did. Verified by
    ;; rendering the window, not by reading the yuck: at 74 characters this
    ;; line ended "...to pull the newest s…" on screen.
"<b>Staying current:</b> <b><span foreground='${palette.cyan}'>ati-update</span></b> pulls the newest settings.")

    ;; ----------------------------
    ;; Actions
    ;; ----------------------------
    (box
      :class "onb-footer"
      :orientation "h"
      :space-evenly false

      (onb-steps :current 5)
      (box :hexpand true)

      (box
      :halign "end"
      :spacing 10

      (button
        :class "onb-btn"
        :timeout "3000ms"
        :onclick "sh -c 'eww close onboarding-welcome bar-tooltip onboarding-workspaces onboarding-keybindings onboarding-finish 2>/dev/null; eww open onboarding-keybindings'"
        "Back")

      (button
        :class "onb-btn onb-btn-Apply"
        :timeout "3000ms"
        :onclick "sh -c 'eww close onboarding-welcome bar-tooltip onboarding-workspaces onboarding-keybindings onboarding-finish 2>/dev/null; qtile cmd-obj -o widget tooltip_widgetbox -f close 2>/dev/null; true'"
        "Finish")
    ))
  )
)
