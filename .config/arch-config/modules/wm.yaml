description: Window manager and desktop environment && apps

packages:
  - qtile-extras
  - qt6ct            # Qt apps (telegram) ignore the GTK theme; qt6ct reads
  - qt5ct            # the palette theme-apply writes, so they follow the mode
  - picom-ftlabs-git  # animation-capable fork -- vanilla picom has no animation support at all
  - light
  - betterlockscreen
  - gromit-mpx
  - screenkey
  - espanso-x11
  - network-manager-applet
  - blueman
  - syncthing
