
# ======================
# Repo metadata
# ======================
.git
.gitignore
README.md
LICENSE
NOTICE.md

# The rest of the repo-root prose. README.md was already listed; these were
# not, so `stow .` symlinked all of them into $HOME -- ~/TROUBLESHOOTING.md,
# ~/plan_ui_ux_qtile.md, ~/qtile-veil-HANDOFF.md and the rest, as top-level
# files in a fresh user's home directory. Nothing reads them from there:
# rofi_docs opens them out of $DOTFILES.
#
# The plan_/qtile-/prompt_ files now live in notes/, so one directory entry
# covers them and every future one. TROUBLESHOOTING.md has to stay named:
# rofi_docs reads it from the repo root.
notes
TROUBLESHOOTING.md

# Claude Code's PER-PROJECT state directory. It is not a dotfile, and it is a
# directory stow will fold into the user's existing ~/.claude rather than skip
# -- which is exactly what happened here: ~/.claude/worktrees is a symlink
# back into this repo, inside the user's global Claude config.
.claude

# ======================
# Images / assets
# ======================
IMGS

# The explainer-video sources and their build script. Added to the repo root
# after IMGS/docs were listed here, and missed -- `stow -n .` reported
# "LINK: video => .dotfiles/video", so a fresh install got a ~/video symlink
# in the user's home for the same reason it once got ~/docs.
video

# The GitHub Pages manual. A directory, so stow would link ~/docs into a
# fresh user's home the same way it once linked the repo-root prose. It is
# read on the web (and out of the repo), never out of $HOME.
docs

# ======================
# Scripts (not dotfiles)
# ======================
installScripts

# ======================
# Things that must NOT be stowed
# ======================
.config/arch-config/.git
.config/arch-config/.backup-*
